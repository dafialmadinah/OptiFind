meta {
  name: Selesaikan Laporan
  type: http
  seq: 5
}

put {
  url: {{apiUrl}}/barangs/1/selesaikan
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("should return 200 on success", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("should return success message", function() {
    const data = res.getBody();
    expect(data).to.have.property('message');
  });
}

docs {
  # Selesaikan Laporan Barang
  
  **Authentication Required**
  
  Endpoint ini mengubah status barang menjadi selesai:
  - "Belum Ditemukan" → "Sudah Ditemukan"
  - "Belum Dikembalikan" → "Sudah Dikembalikan"
  
  Replace '1' di URL dengan ID barang yang sebenarnya.
  
  Authorization:
  - Hanya pelapor atau admin yang bisa menyelesaikan laporan
  - Gunakan Bearer token dari response login
}
