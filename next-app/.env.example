NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: Service Role Key (hanya untuk insert user ke database saat registrasi)
# Jika tidak disediakan, akan fallback ke anon key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Authentication - Supabase Auth
# Get your keys from Supabase Dashboard -> Settings -> API
# Note: Anon key is safe to use in frontend, protected by RLS policies

# NextAuth Secret (untuk compatibility)
NEXTAUTH_SECRET=your-secret-key-here-change-this-in-production

# Generate secret key dengan command:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

